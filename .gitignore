# Rust
target/
Cargo.lock.bak

# Server data (live ciphertext blobs — keep out of git)
data.json

# Python — local-only, this repo is Rust
*.py
.reference/
pyproject.toml
uv.lock
.python-version
__pycache__/
.venv
